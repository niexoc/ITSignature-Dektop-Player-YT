### Listens to ITS Platinum Player (Desktop) [itsignature://] protocol, decrypts data, and opens the video directly in YouTube.

---
**IMPORTANT:** Change the path of the `its_handler.bat` in the last line of `register_protocol.reg` (double backslashes are important!) and double click `register_protocol.reg` to add the protocol to the registry.

---
Handling antivirus is your job!

---
Even though here I provide the batch and registry files for windows, the logic behind the batch file is working on any platform. Use AI and build it for linux or mac if you want!
